import React from "react";

const TableContainerFilters = ({ children }) => {
	return <div className="my-2 flex sm:flex-row flex-col gap-2">{children}</div>;
};

export default TableContainerFilters;
